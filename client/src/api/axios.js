// axios.js — convenience re-export so imports like `import API from './api/axios'` work
export { default, registerUser, loginUser, getUserProfile, getAllUsers, fetchProducts, fetchProduct, createProduct, updateProduct, deleteProduct, placeOrder, getMyOrders, getAllOrders, getOrderById, updateOrderStatus } from './index';
